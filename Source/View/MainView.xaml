<UserControl x:Class="RollingLineSavegameFix.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"                          
             xmlns:vm="clr-namespace:RollingLineSavegameFix.ViewModel"             
             Margin="20">
    <UserControl.DataContext>
        <vm:MainViewModel />
    </UserControl.DataContext>
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <TextBox MinWidth="300" Text="{Binding FileName}"/>
            <Button Content="Open File" Click="OpenFileBrowser_Click" />
        </StackPanel>
        
            <GroupBox Header="Waggons"  IsEnabled="{Binding AreOptionsAvaiable}">
                <StackPanel Orientation="Vertical" >
                    <RadioButton Content="Remove Faulty Waggons" />
                    <RadioButton Content="Remove All Waggons" />
                </StackPanel>
            </GroupBox>
        <GroupBox Header="Breaks" IsEnabled="{Binding AreOptionsAvaiable}">
            <CheckBox Content="Remove Breaks"/>
        </GroupBox>
        <Button Content="DO IT NOW!!!" IsEnabled="{Binding AreOptionsAvaiable}"/>
    </StackPanel>
</UserControl>
